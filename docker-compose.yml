services:
  api:
    image: daun104/project2-api:latest
    ports:
      - "3000:3000"
    env_file:
      - .env.docker
    depends_on:
      - mongo

  mongo:
    image: mongo:6
    restart: always
    ports:
      - "27017:27017"
